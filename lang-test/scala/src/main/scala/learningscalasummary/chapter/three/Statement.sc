// A statement is just an expression that doesn't return a value. It means, return type is `Unit`.
println("I'm statement, return type is unit")
val x = "I'm also statement, assignment is statement. So when user make a fault miss '=' during use compare in if block, it doesn't a matter because if require expression which return value"