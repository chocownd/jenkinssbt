pipeline {
    agent {
        docker {
//             image 'mozilla/sbt'
            image 'openjdk:8'
            args '-v $HOME/.cache:/.cache'
        }
    }
    stages {
        stage('Install sbt') {
            steps {
                sh 'ls -lah'
                sh 'wget https://github.com/sbt/sbt/releases/download/v1.4.4/sbt-1.4.4.tgz'
                sh 'tar -zxvf sbt-1.4.4.tgz'
                sh 'ls -lah'
                sh 'env'
                sh 'export PATH=$PATH:$PWD/sbt/bin'
                sh 'env'
                sh 'sbt --version'
            }
        }
        stage('Sbt version') {
            steps {
                sh 'sbt --version'
            }
        }
        stage('Clean') {
            steps {
                sh 'sbt "langTest/clean"'
            }
        }
        stage('Compile') {
            steps {
                sh 'sbt "langTest/compile"'
            }
        }
        stage('Run') {
            steps {
                sh 'sbt "langTest/run"'
            }
        }
        stage('Uber jar') {
            steps {
                sh 'sbt assembly'
            }
        }
        stage('Find Uber jar') {
            steps {
                sh 'ls -lah'
            }
        }
        stage('Where am I') {
            steps {
                sh 'pwd'
            }
        }
        stage('Execute Uber jar') {
            steps {
                sh 'java -jar ./lang-test/scala/target/scala-2.12/langTest-assembly-0.1.0-SNAPSHOT.jar'
            }
        }
    }
}
