pipeline {
    agent {
        docker {
            image 'mozilla/sbt'
            args '-v $HOME/.cache:/.cache'
        }
    }
    stages {
        stage('Clean') {
            steps {
                sh 'sbt "langTest/clean"'
            }
        }
        stage('Compile') {
            steps {
                sh 'sbt "langTest/compile"'
            }
        }
        stage('Run') {
            steps {
                sh 'sbt "langTest/run"'
            }
        }
    }
}
